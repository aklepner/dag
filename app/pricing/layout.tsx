import React from "react"
import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Partnership: We Don't Take Clients. We Select Partners.",
  description:
    "Three partnership levels with the full Recipe + Rhythm methodology. Foundation, Growth, and Scale. See what doing nothing costs, then book a Fit Assessment Call.",
}

export default function PricingLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
